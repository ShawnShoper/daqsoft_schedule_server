package daqsoft_schedule_server.model.timeschedule;

import org.junit.Test;

public class TimingSchedule_Test
{
	@Test
	public void test() throws Exception
	{
		// TimingSchedule quartzSchedule = new TimingSchedule();
		// {
		// Task task = new Task();
		// task.setCreateTime(new Date());
		// task.setCronExpress("*/5 * * * * ?");
		// task.setTiming(true);
		// task.setId("test");
		// quartzSchedule.schedule(task);
		// }
		// {
		// Task task = new Task();
		// task.setCreateTime(new Date());
		// task.setCronExpress("*/3 * * * * ?");
		// task.setTiming(true);
		// task.setId("test4");
		// quartzSchedule.schedule(task);
		// }
		// TimeUnit.MINUTES.sleep(10);
	}
}
